{
  "name": "tab-master",
  "version": "1.0.0",
  "description": "A Chrome extension that automatically saves your browser session (tabs and tab groups) when Chrome closes and allows you to restore them with a keyboard shortcut.",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "canvas": "^3.1.2"
  }
}
